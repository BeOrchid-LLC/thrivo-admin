/** Name of the backend-issued httpOnly admin session cookie. */
export const SESSION_COOKIE = "thrivo_admin_session";

/** Default number of rows per page across all admin tables. */
export const DEFAULT_PAGE_SIZE = 12;
