# thrivo-admin

Admin dashboard for Thrivo.

**Stack:** Next.js/TanStack Start (TBD) · TypeScript · Zod

**Port:** `http://localhost:3001` (or `3000` if run alone)

## Getting started

```bash
git init
npm install
npm run dev -p 3001
```

See `../CLAUDE.md` for conventions and stack details.
